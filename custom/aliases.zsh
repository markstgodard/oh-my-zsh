# general time savers
alias v="vim"
alias vi="vim"
alias cls="clear"
alias h="history"

#  Cloud Foundry
alias dammit='bosh create release --force && bosh target 192.168.50.4 lite && ./bosh-lite/make_manifest && bosh -n upload release && bosh -n deploy'

# Ruby / Bundler  (may be able to take this out since including ruby plugin
# in OhMyZsh
alias be='bundle exec'

